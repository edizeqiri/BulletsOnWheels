[package]
name = "magic_dodgeball"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.1", default-features = true }

core_engine = { path = "../../core_engine" }
bevy_ecs_ldtk = "0.15.0"

[workspace]
